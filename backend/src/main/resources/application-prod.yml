spring:
  jpa:
    show-sql: false

logging:
  level:
    com.school.attendance: INFO
    org.springframework.web: WARN

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
